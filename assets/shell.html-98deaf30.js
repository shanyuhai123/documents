import{_ as e,p as a,q as s,a1 as n}from"./framework-49860b1b.js";const l={},c=n(`<h1 id="shell-脚本" tabindex="-1"><a class="header-anchor" href="#shell-脚本" aria-hidden="true">#</a> Shell 脚本</h1><p>一点点追加。</p><h2 id="bash-变量" tabindex="-1"><a class="header-anchor" href="#bash-变量" aria-hidden="true">#</a> Bash 变量</h2><p>Bash 变量分为：环境变量和自定义变量。</p><h3 id="环境变量" tabindex="-1"><a class="header-anchor" href="#环境变量" aria-hidden="true">#</a> 环境变量</h3><p>环境变量是 Bash 环境自带的变量，进入 Shell 时已经定义好了，它们通常时系统定义好的，也可以由用户从父 Shell 传入子 Shell。</p><h3 id="自定义变量" tabindex="-1"><a class="header-anchor" href="#自定义变量" aria-hidden="true">#</a> 自定义变量</h3><p>自定义变量是用户在当前 Shell 里自己定义的变量，仅在当前 Shell 可用。一旦退出 Shell，该变量就不存在了。</p><h3 id="特殊变量-脚本内" tabindex="-1"><a class="header-anchor" href="#特殊变量-脚本内" aria-hidden="true">#</a> 特殊变量 - 脚本内</h3><p>脚本文件内部可以使用特殊变量：</p><ul><li><code>$0</code>：脚本文件名，即 <code>script.sh</code></li><li><code>$1~$9</code>：对应脚本的第一个参数到第九个参数</li><li><code>$#</code>：参数的总数</li><li><code>$@</code>：全部的参数，参数之间使用空格分隔</li></ul><p>解析一个示例：</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token assign-left variable">dir</span><span class="token operator">=</span><span class="token variable"><span class="token variable">\`</span><span class="token builtin class-name">cd</span> <span class="token punctuation">$(</span>dirname $0<span class="token punctuation">)</span> <span class="token operator">&amp;&amp;</span> <span class="token builtin class-name">pwd</span><span class="token variable">\`</span></span> <span class="token comment"># 注意等号两边不要有空格</span>
<span class="token comment"># $0 =&gt; 即脚本文件名（包含你进入的目录）./shells/echo.sh</span>
<span class="token comment"># $(dirname $0) =&gt; 输出文件目录，包裹住使得不会被 \`echo\` 解析成两部分 ./shells</span>
<span class="token comment"># 完成的 =&gt; 进入目录，并获取其完整路径 /home/shanyuhai/code/shells</span>
</code></pre></div><h2 id="条件语句" tabindex="-1"><a class="header-anchor" href="#条件语句" aria-hidden="true">#</a> 条件语句</h2><h3 id="if" tabindex="-1"><a class="header-anchor" href="#if" aria-hidden="true">#</a> if</h3><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token keyword">if</span> <span class="token punctuation">[</span> expression <span class="token punctuation">]</span>
<span class="token keyword">then</span>
   Statement<span class="token punctuation">(</span>s<span class="token punctuation">)</span> to be executed <span class="token keyword">if</span> expression is <span class="token boolean">true</span>
<span class="token keyword">fi</span>

<span class="token comment"># if expr1 ?then? body1 elseif expr2 ?then? body2 elseif ... ?else? ?bodyN?</span>
</code></pre></div><p><code>then</code> 语句中为常规执行语句，而 <code>expression</code> 判断表达式提供了一些快捷判断。</p><h4 id="_1-文件判断" tabindex="-1"><a class="header-anchor" href="#_1-文件判断" aria-hidden="true">#</a> 1. 文件判断</h4><ul><li><code>[ -a file ]</code>：如果 <code>file</code> 存在，则为 true</li><li><code>[ -d file ]</code>：如果 <code>file</code> 存在且是一个目录，则为 true</li><li><code>[ -f file ]</code>：如果 <code>file</code> 存在且是一个普通文件，则为 true</li></ul><h4 id="_2-字符串判断" tabindex="-1"><a class="header-anchor" href="#_2-字符串判断" aria-hidden="true">#</a> 2. 字符串判断</h4><ul><li><code>[ -n string ]</code>：如果字符串 <code>string</code> 长度大于 0，则为 true</li><li><code>[ -z string ]</code>：如果字符串 <code>string</code> 长度为 0，则为 true</li></ul>`,21),o=[c];function d(t,i){return a(),s("div",null,o)}const r=e(l,[["render",d],["__file","shell.html.vue"]]);export{r as default};
