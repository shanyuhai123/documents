import{d as e}from"./app.41f386f9.js";const r={},o=e('<h2 id="介绍" tabindex="-1"><a class="header-anchor" href="#介绍" aria-hidden="true">#</a> 介绍</h2><p>虽然可以使用 <code>webpack</code> 的 <code>Hot Module</code> 来实时更新，但对于写一些简单的 CSS 用例来说则显得麻烦了一些。</p><h2 id="简单使用" tabindex="-1"><a class="header-anchor" href="#简单使用" aria-hidden="true">#</a> 简单使用</h2><p>在 <code>EXTENSIONS</code> 中搜索 <code>Live Server</code> 并安装，之后即可在 Status Bar 上看到新增的 <code>Go Live</code> 图标，点击即可启动它。</p><blockquote><p>需注意，这一切都是在常规的 CSS 用例下使用，若考虑更复杂的情况为什么不直接 <code>webpack</code> 而来折腾自己呢？</p></blockquote><h2 id="配合-browser-preview" tabindex="-1"><a class="header-anchor" href="#配合-browser-preview" aria-hidden="true">#</a> 配合 Browser Preview</h2><p>在具有双屏时直接使用 <code>Live Server</code> 即可获取良好的体验，但仅有一个屏幕时频繁的切换窗口也会带来糟糕的体验。</p><p>解决的方案就是安装 <code>Browser Preview</code>，然后打开 <code>Settings</code> 搜索 <code>useBrowserPreview</code>，勾选即可。</p>',8);r.render=function(e,r){return o};export default r;
